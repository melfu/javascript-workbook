'use strict'
const names = ['mel', 'hannah', 'laura', 'wilson', 'stiles'];
const printListOfOlympians = (array) => {
    array.forEach(function(element){
        console.log(element)
    })
}
printListOfOlympians(names);
